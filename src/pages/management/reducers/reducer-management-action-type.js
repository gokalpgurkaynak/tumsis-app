// {ForAction} should be replaced for which action TYPE this reducer is defined
export default function reducerManagementForAction (state = [], action) {
  switch (action.type) {
    default:
        return state;
      break;
  }
}
